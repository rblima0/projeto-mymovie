/* CONEXAO COM A API */
import { API_KEY_V1, MOVIE_URL_LIST } from './keys'

/* export function fetchMovieList() {
    let url = MOVIE_URL_LIST + API_KEY_V1
    
    return fetch(url)
        .then(response => response.json())
        .then(json => json.results)
} */